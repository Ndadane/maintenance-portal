<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <RootNamespace>MaintenancePortal.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\MaintenancePortal.Api\MaintenancePortal.Api.csproj" />
    <ProjectReference Include="..\..\src\MaintenancePortal.Core\MaintenancePortal.Core.csproj" />
    <ProjectReference Include="..\..\src\MaintenancePortal.Infrastructure\MaintenancePortal.Infrastructure.csproj" />
  </ItemGroup>

  <!--
    Test framework packages (xUnit, Microsoft.NET.Test.Sdk, WebApplicationFactory,
    Testcontainers for Postgres) are added via `dotnet add package` — see
    SETUP_COMMANDS.md. Added later, in Phase 3/11 per the build order, once
    there's something worth testing.
  -->

</Project>
